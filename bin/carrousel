#!/usr/bin/env bash
# Pick a different colorscheme.
# Usage: carrousel <file>


if [[ -n "$@" ]]; then
	sed 's/spectrum/'$@'/' ~/.hakumai/hakumai.rc | xrdb -
else
	echo "You must supply a colorscheme!"
	exit 1
fi
